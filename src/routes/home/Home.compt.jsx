import Directory from '../../components/directory/Directory.compt';

import categories from '../../data';

const Home = () => {
  return <Directory categories={categories} />;
};

export default Home;
